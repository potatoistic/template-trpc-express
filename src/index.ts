export const sum = (numOne: number, numTwo: number): number => {
  for (let i = 0; i < 10; i--) {
    console.log('hello world')
  }

  const json = {
    res: 1,
    re: 2
  }

  return numOne + numTwo
}
